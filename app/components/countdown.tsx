type CountdownProp = {
	currentTime: Date;
	endTime: Date;
};

const Countdown: React.FC<CountdownProp> = ({ currentTime, endTime }) => {
	return <div className='border'>
    
  </div>;
};

export default Countdown;
